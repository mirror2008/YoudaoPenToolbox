<Window x:Class="YoudaoPenToolbox.Views.RemoteFileViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="文件查看"
        Width="1100" Height="720"
        MinWidth="860" MinHeight="480"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource RegionBrush}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="ViewerListBoxItemStyle" TargetType="ListBoxItem">
                <Setter Property="Padding" Value="2,0"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            </Style>
            <Style x:Key="ViewerListBoxStyle" TargetType="ListBox">
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="FontFamily" Value="Consolas"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
                <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
                <Setter Property="VirtualizingPanel.IsContainerVirtualizable" Value="True"/>
                <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
                <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
                <Setter Property="ItemContainerStyle" Value="{StaticResource ViewerListBoxItemStyle}"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap"/>
        <TextBlock Grid.Row="1" x:Name="InfoText" Margin="0,6,0,12" FontSize="12"
                   Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap"/>

        <Grid Grid.Row="2">
            <Border x:Name="BinaryViewBorder" Visibility="Collapsed"
                    Background="{DynamicResource SecondaryRegionBrush}" CornerRadius="6" Padding="4">
                <ListBox x:Name="BinaryListBox" Style="{StaticResource ViewerListBoxStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="220"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding HexText, Mode=OneWay}" TextTrimming="None"/>
                                <TextBlock Grid.Column="1" Text="{Binding AsciiText, Mode=OneWay}" Margin="12,0,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <Border x:Name="TextViewBorder" Visibility="Collapsed"
                    Background="{DynamicResource SecondaryRegionBrush}" CornerRadius="6" Padding="4">
                <ListBox x:Name="TextListBox" Style="{StaticResource ViewerListBoxStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ., Mode=OneWay}" TextWrapping="NoWrap"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <Border x:Name="LoadingPanel" Background="#CC1E1E1E" CornerRadius="6">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="220" Height="4"/>
                    <TextBlock x:Name="LoadingText" Text="正在后台加载内容..." Margin="0,12,0,0"
                               HorizontalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>

<Window x:Class="YoudaoPenToolbox.Views.AppMessageBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="480"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        FontFamily="Segoe UI, Microsoft YaHei UI"
        Background="{DynamicResource RegionBrush}">
    <Border x:Name="DialogRoot" Style="{StaticResource DialogCard}" Margin="28" MaxWidth="520">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border x:Name="IconBadge" Grid.Row="0" Grid.RowSpan="2"
                    Width="46" Height="46" CornerRadius="23"
                    Margin="0,2,18,0" VerticalAlignment="Top">
                <TextBlock x:Name="IconText" FontSize="22" FontWeight="SemiBold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <TextBlock x:Name="TitleText" Grid.Column="1" Grid.Row="0"
                       Style="{StaticResource DialogTitle}" TextWrapping="Wrap"/>

            <ScrollViewer Grid.Column="1" Grid.Row="1" MaxHeight="340" Margin="0,10,0,0"
                          VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="14"
                           LineHeight="22" Foreground="{DynamicResource PrimaryTextBrush}"/>
            </ScrollViewer>

            <StackPanel x:Name="ButtonPanel" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2"
                        Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0"/>
        </Grid>
    </Border>
</Window>

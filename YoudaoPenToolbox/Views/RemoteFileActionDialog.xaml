<Window x:Class="YoudaoPenToolbox.Views.RemoteFileActionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="文件操作"
        Width="520"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource RegionBrush}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Margin="20" Padding="20" CornerRadius="8"
            Background="{DynamicResource SecondaryRegionBrush}">
        <StackPanel>
            <TextBlock Text="选择操作" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>

            <Border Padding="12,10" CornerRadius="6" Background="{DynamicResource RegionBrush}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock x:Name="FileNameText" FontSize="15" FontWeight="SemiBold" TextWrapping="Wrap"/>
                    <TextBlock x:Name="FilePathText" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}"
                               TextWrapping="Wrap" Margin="0,6,0,0"/>
                    <TextBlock x:Name="FileSizeText" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <TextBlock Text="请选择对该文件执行的操作：" FontSize="12"
                       Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,12"/>

            <StackPanel>
                <Button Content="下载到电脑..." Height="40" Margin="0,0,0,8"
                        HorizontalContentAlignment="Left" Padding="16,0"
                        Click="Download_Click"/>
                <Button Content="记事本打开（内置）" Height="40" Margin="0,0,0,8"
                        HorizontalContentAlignment="Left" Padding="16,0"
                        Click="OpenText_Click"/>
                <Button Content="二进制查看（十六进制 + ASCII）" Height="40" Margin="0,0,0,8"
                        HorizontalContentAlignment="Left" Padding="16,0"
                        Click="OpenBinary_Click"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="取消" Width="96" Height="36" IsCancel="True"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
